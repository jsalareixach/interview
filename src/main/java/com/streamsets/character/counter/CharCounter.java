package com.streamsets.character.counter;

public class CharCounter {

  /**
   * Counts the number of occurences of a character in the given string
   * @param inputString the input string to count characters from
   * @param character the character to search for and count
   */
  public int countCharOccurences(String inputString, char character) {
    return 0;
  }
}